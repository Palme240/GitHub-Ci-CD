service: GitHubActions

provider:
  name: azure
  runtime: python3.7
  location: westeurope
  
functions:
  function1:
    handler: Azure_function.pipeline_main
    events:
     - http: true
       x-azure-settings:
          authLevel : anonymous
     - http: true
       x-azure-settings:
          direction: out
          name: res
